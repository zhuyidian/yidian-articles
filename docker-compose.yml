services:
  articles-site:
    build: .
    container_name: yidianhub-articles
    restart: unless-stopped
    networks:
      caddy_frontend:
        aliases:
          - yidianhub-articles

networks:
  caddy_frontend:
    external: true
    name: yidian-api_frontend
